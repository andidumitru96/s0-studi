.project-card {
  aspect-ratio: 3 / 2;
}

@media (max-width: 768px) {
  .project-card {
    aspect-ratio: auto;
  }
}

@media (min-width: 769px) {
  .project-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 16px;
  }
}